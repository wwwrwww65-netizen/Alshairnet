var $jscomp = $jscomp || {};
(($jscomp.scope = {}),
  ($jscomp.createTemplateTagFirstArg = function (e) {
    return (e.raw = e);
  }),
  ($jscomp.createTemplateTagFirstArgWithRaw = function (e, t) {
    return ((e.raw = t), e);
  }),
  ($jscomp.arrayIteratorImpl = function (e) {
    var t = 0;
    return function () {
      return t < e.length ? { done: !1, value: e[t++] } : { done: !0 };
    };
  }),
  ($jscomp.arrayIterator = function (e) {
    return { next: $jscomp.arrayIteratorImpl(e) };
  }),
  ($jscomp.makeIterator = function (e) {
    var t =
      "undefined" != typeof Symbol && Symbol.iterator && e[Symbol.iterator];
    return t ? t.call(e) : $jscomp.arrayIterator(e);
  }),
  ($jscomp.arrayFromIterator = function (e) {
    for (var t, n = []; !(t = e.next()).done;) n.push(t.value);
    return n;
  }),
  ($jscomp.arrayFromIterable = function (e) {
    return e instanceof Array
      ? e
      : $jscomp.arrayFromIterator($jscomp.makeIterator(e));
  }),
  ($jscomp.checkStringArgs = function (e, t, n) {
    if (null == e)
      throw new TypeError(
        "The 'this' value for String.prototype." +
          n +
          " must not be null or undefined",
      );
    if (t instanceof RegExp)
      throw new TypeError(
        "First argument to String.prototype." +
          n +
          " must not be a regular expression",
      );
    return e + "";
  }),
  ($jscomp.ASSUME_ES5 = !1),
  ($jscomp.ASSUME_NO_NATIVE_MAP = !1),
  ($jscomp.ASSUME_NO_NATIVE_SET = !1),
  ($jscomp.SIMPLE_FROUND_POLYFILL = !1),
  ($jscomp.ISOLATE_POLYFILLS = !1),
  ($jscomp.FORCE_POLYFILL_PROMISE = !1),
  ($jscomp.FORCE_POLYFILL_PROMISE_WHEN_NO_UNHANDLED_REJECTION = !1),
  ($jscomp.defineProperty =
    $jscomp.ASSUME_ES5 || "function" == typeof Object.defineProperties
      ? Object.defineProperty
      : function (e, t, n) {
          return (
            e == Array.prototype || e == Object.prototype || (e[t] = n.value),
            e
          );
        }),
  ($jscomp.getGlobal = function (e) {
    e = [
      "object" == typeof globalThis && globalThis,
      e,
      "object" == typeof window && window,
      "object" == typeof self && self,
      "object" == typeof global && global,
    ];
    for (var t = 0; t < e.length; ++t) {
      var n = e[t];
      if (n && n.Math == Math) return n;
    }
    throw Error("Cannot find global object");
  }),
  ($jscomp.global = $jscomp.getGlobal(this)),
  ($jscomp.IS_SYMBOL_NATIVE =
    "function" == typeof Symbol && "symbol" == typeof Symbol("x")),
  ($jscomp.TRUST_ES6_POLYFILLS =
    !$jscomp.ISOLATE_POLYFILLS || $jscomp.IS_SYMBOL_NATIVE),
  ($jscomp.polyfills = {}),
  ($jscomp.propertyToPolyfillSymbol = {}),
  ($jscomp.POLYFILL_PREFIX = "$jscp$"));
var $jscomp$lookupPolyfilledValue = function (e, t) {
  var n = $jscomp.propertyToPolyfillSymbol[t];
  return null == n ? e[t] : void 0 !== (n = e[n]) ? n : e[t];
};
(($jscomp.polyfill = function (e, t, n, o) {
  t &&
    ($jscomp.ISOLATE_POLYFILLS
      ? $jscomp.polyfillIsolated(e, t, n, o)
      : $jscomp.polyfillUnisolated(e, t, n, o));
}),
  ($jscomp.polyfillUnisolated = function (e, t, n, o) {
    for (n = $jscomp.global, e = e.split("."), o = 0; o < e.length - 1; o++) {
      var r = e[o];
      if (!(r in n)) return;
      n = n[r];
    }
    (t = t((o = n[(e = e[e.length - 1])]))) != o &&
      null != t &&
      $jscomp.defineProperty(n, e, {
        configurable: !0,
        writable: !0,
        value: t,
      });
  }),
  ($jscomp.polyfillIsolated = function (e, t, n, o) {
    var r = e.split(".");
    ((e = 1 === r.length),
      (o = r[0]),
      (o = !e && o in $jscomp.polyfills ? $jscomp.polyfills : $jscomp.global));
    for (var i = 0; i < r.length - 1; i++) {
      var s = r[i];
      if (!(s in o)) return;
      o = o[s];
    }
    ((r = r[r.length - 1]),
      null !=
        (t = t((n = $jscomp.IS_SYMBOL_NATIVE && "es6" === n ? o[r] : null))) &&
        (e
          ? $jscomp.defineProperty($jscomp.polyfills, r, {
              configurable: !0,
              writable: !0,
              value: t,
            })
          : t !== n &&
            (void 0 === $jscomp.propertyToPolyfillSymbol[r] &&
              ((n = (1e9 * Math.random()) >>> 0),
              ($jscomp.propertyToPolyfillSymbol[r] = $jscomp.IS_SYMBOL_NATIVE
                ? $jscomp.global.Symbol(r)
                : $jscomp.POLYFILL_PREFIX + n + "$" + r)),
            $jscomp.defineProperty(o, $jscomp.propertyToPolyfillSymbol[r], {
              configurable: !0,
              writable: !0,
              value: t,
            }))));
  }),
  $jscomp.polyfill(
    "String.prototype.repeat",
    function (e) {
      return (
        e ||
        function (e) {
          var t = $jscomp.checkStringArgs(this, null, "repeat");
          if (0 > e || 1342177279 < e)
            throw new RangeError("Invalid count value");
          e |= 0;
          for (var n = ""; e;) (1 & e && (n += t), (e >>>= 1) && (t += t));
          return n;
        }
      );
    },
    "es6",
    "es3",
  ),
  $jscomp.polyfill(
    "Object.is",
    function (e) {
      return (
        e ||
        function (e, t) {
          return e === t ? 0 !== e || 1 / e == 1 / t : e != e && t != t;
        }
      );
    },
    "es6",
    "es3",
  ),
  $jscomp.polyfill(
    "Array.prototype.includes",
    function (e) {
      return (
        e ||
        function (e, t) {
          var n = this;
          n instanceof String && (n = String(n));
          var o = n.length,
            r = t || 0;
          for (0 > r && (r = Math.max(r + o, 0)); r < o; r++) {
            var i = n[r];
            if (i === e || Object.is(i, e)) return !0;
          }
          return !1;
        }
      );
    },
    "es7",
    "es3",
  ),
  $jscomp.polyfill(
    "String.prototype.includes",
    function (e) {
      return (
        e ||
        function (e, t) {
          return (
            -1 !==
            $jscomp.checkStringArgs(this, e, "includes").indexOf(e, t || 0)
          );
        }
      );
    },
    "es6",
    "es3",
  ));
var $loginData,
  animating = !1,
  submitPhase2 = 500,
  logoutPhase1 = 20,
  $login = document.getElementById("login"),
  $status = document.getElementById("status"),
  $loginSubmit = document.querySelector(".login-submit"),
  loggedIn = !1,
  intervalStatus = !1,
  statusIntervalTimer = null,
  isLoggingOut = !1,
  isChangingSpeed = !1,
  speedChangeSafetyTimer = null,
  $passwordField = document.login.querySelector("input[password-field]"),
  $advImage = document.querySelector("[hot-adv-img]"),
  errorCount = 0,
  $errorContainer = document.querySelector(".error-container");
function onLoginSubmitted() {
  isLoggingOut = false;
  var e = document.getElementById("error");
  if (!document.login || !document.login.username || 1 > document.login.username.value.length) {
    if (e) e.innerText = "يجب ادخال قيمة صحيحة!";
    showErrorPopup();
    animating = false;
  } else {
    if (e) e.innerText = "";
    var allLoginBtns = document.querySelectorAll(".login-submit, #mainLoginBtn");
    allLoginBtns.forEach(function (btn) {
      btn.classList.add("processing");
    });
    userLogin();
  }
}
function onLogoutSubmitted(e, t) {
  isLoggingOut = true;
  loggedIn = false;
  animating = false;
  intervalStatus = false;
  try {
    localStorage.setItem("hotspot_logged_in", "no");
    localStorage.removeItem("hotspot_status_cache");
  } catch (err) {}
  if (statusIntervalTimer) {
    clearInterval(statusIntervalTimer);
    statusIntervalTimer = null;
  }
  var allLoginBtns = document.querySelectorAll(".login-submit, .button, #mainLoginBtn");
  allLoginBtns.forEach(function (btn) {
    btn.classList.remove("processing", "loading", "clicked", "success");
    btn.removeAttribute("disabled");
  });
  var isErase = e && e.hasAttribute && e.hasAttribute("erase-cookie");
  if (isErase) {
    if (typeof clearCookies === "function") clearCookies();
    if (document.login) {
      var uInput = document.login.querySelector("input[username-field]");
      if (uInput) uInput.value = "";
      var pInput = document.login.querySelector("input[password-field]");
      if (pInput) pInput.value = "";
    }
  } else {
    var cookieObj = typeof getLoginCardCookie === "function" ? getLoginCardCookie() : { username: "" };
    if (cookieObj && cookieObj.username && document.login) {
      var uInput = document.login.querySelector("input[username-field]");
      if (uInput) uInput.value = cookieObj.username;
    }
  }

  // Reset status speed trigger & select
  var triggerText = document.getElementById("statusSpeedTriggerText");
  if (triggerText) triggerText.textContent = "أضغط هنا لتغيير سرعة الإنترنت";
  var speedchange = document.getElementById("speedchange");
  if (speedchange) speedchange.value = "ns";

  // Reset login speed selector to default
  if (typeof hotspotConfig !== "undefined" && hotspotConfig.speeds) {
    var defaultSp = hotspotConfig.speeds.find(function(s) { return s.visible && s.isDefault; }) || hotspotConfig.speeds[0];
    if (defaultSp) {
      var speedSel = document.getElementById("speed");
      var speedDisp = document.getElementById("selectedSpeedDisplay");
      if (speedSel) speedSel.value = defaultSp.value;
      if (speedDisp) speedDisp.textContent = defaultSp.name;
      var allPills = document.querySelectorAll("#speedPillsRow .speed-pill-btn");
      allPills.forEach(function(p) {
        p.classList.toggle("active", p.getAttribute("data-speed") === defaultSp.value);
      });
      var modalCards = document.querySelectorAll("#speedModalList .speed-card-option");
      modalCards.forEach(function(c) {
        c.classList.toggle("active", c.getAttribute("data-speed") === defaultSp.value);
      });
    }
  }

  // Immediately hide status page and show login page
  hideStatusPage();
  showLoginPage();

  var logoutMsg = isErase ? "تم تسجيل الخروج بنجاح" : "تم قطع اتصالك بنجاح";
  var errEl = document.getElementById("error");
  if (errEl) {
    errEl.innerText = logoutMsg;
  }
  showErrorPopup();

  userLogout(isErase);
  setTimeout(function () {
    isLoggingOut = false;
  }, 1000);
}
function showParentElement(e, t) {
  if (e) {
    e.classList.remove("processing");
    e.classList.remove("success");
  }
  if (t && t.id !== "status") {
    if (typeof window.openAppModal === "function") {
      window.openAppModal(t);
    } else {
      t.style.display = "flex";
      t.classList.add("active");
    }
    animating = false;
    return;
  }
  if (t && t.id === "status") {
    showStatusPage();
    if (window.ModalScrollLock) {
      window.ModalScrollLock.unlock(true);
    }
    document.documentElement.classList.remove("modal-scroll-locked");
    document.body.classList.remove("modal-scroll-locked");
    animating = false;
  }
}
function hideLoginPage() {
  if ($login) {
    $login.style.display = "none";
    $login.classList.add("inactive");
    $login.classList.remove("active");
  }
}
function hideStatusPage() {
  if (isChangingSpeed) return;
  if ($status) {
    $status.style.display = "none";
    $status.classList.remove("active");
  }
}
function showLoginPage() {
  if (isChangingSpeed) return;
  animating = false;
  document.documentElement.classList.remove("is-mTik-logged-in");
  document.documentElement.classList.add("is-mTik-logged-out");
  if (window.ModalScrollLock) {
    window.ModalScrollLock.unlock();
  }

  // Reset login button loading states
  var allLoginBtns = document.querySelectorAll(".login-submit, .button, #mainLoginBtn");
  allLoginBtns.forEach(function (btn) {
    btn.classList.remove("processing", "loading", "clicked", "success");
    btn.removeAttribute("disabled");
  });

  if (loggedIn) {
    showStatusPage();
  } else {
    populateSelectWithOptions();
    if ($status) {
      $status.style.display = "none";
      $status.classList.remove("active");
    }
    if ($login) {
      $login.style.visibility = "visible";
      $login.style.display = "block";
      $login.classList.remove("inactive");
      $login.classList.add("active");
    }
  }
}
function showStatusPage() {
  document.documentElement.classList.remove("is-mTik-logged-out");
  document.documentElement.classList.add("is-mTik-logged-in");
  if ($login) {
    $login.style.display = "none";
    $login.classList.add("inactive");
    $login.classList.remove("active");
  }
  if ($status) {
    if ($status.style.display !== "block") {
      $status.style.display = "block";
    }
    if (!$status.classList.contains("active")) {
      $status.classList.add("active");
    }
  }
}
function initHotspotEvents(e, t) {
  if (!e || !t) return;
  e.addEventListener("click", function (ev) {
    if (!animating) {
      animating = true;
      if (t.id === "status") {
        onLoginSubmitted();
      } else {
        if (t.id === "app-store") loadScript("js/store.min.js");
        this.classList.add("processing");
        showParentElement(this, t);
      }
    }
  });

  var o = t.querySelectorAll(".app-logout");
  o.forEach(function (r) {
    r.addEventListener("click", function (ev) {
      if (t.id === "status") {
        if (!animating) {
          animating = true;
          var n = this;
          onLogoutSubmitted(n, t);
          n.classList.add("clicked");
          setTimeout(function () {
            t.classList.remove("active");
            t.style.display = "none";
            showLoginPage();
            animating = false;
            n.classList.remove("clicked");
          }, 180);
        }
      } else {
        ev.preventDefault();
        ev.stopPropagation();
        if (typeof window.closeAppModal === "function") {
          window.closeAppModal(t);
        } else {
          t.classList.remove("active");
          t.style.display = "none";
        }
      }
    });
  });
}
function enc(e) {
  var t = "";
  const n = e.split(" ");
  for (e = 0; e < n.length; e++) t += String.fromCharCode(parseInt(n[e]));
  return t;
}
function toArabicSpeed(e) {
  if (!e || e === "" || e === "undefined") {
    if (typeof hotspotConfig !== "undefined" && hotspotConfig.speeds) {
      var defaultSp = hotspotConfig.speeds.find(function(s) { return s.isDefault; });
      if (defaultSp) return defaultSp.name;
    }
    return "السرعة الافتراضية";
  }
  if (typeof hotspotConfig !== "undefined" && hotspotConfig.speeds) {
    var foundSp = hotspotConfig.speeds.find(function(s) {
      return s.value && (s.value.toLowerCase() === String(e).toLowerCase());
    });
    if (foundSp) return foundSp.name;
  }
  const t = document.getElementById("speed");
  if (t && t.options) {
    for (var n = 0; n < t.options.length; n++) {
      if (t.options[n].value && t.options[n].value.toLowerCase() === String(e).toLowerCase()) return t.options[n].text;
    }
  }
  return e;
}
function syncCurrentSpeedUI(speedVal) {
  speedVal = (speedVal === undefined || speedVal === null || speedVal === "undefined" || speedVal === "null") ? "" : String(speedVal).trim();
  var speedName = toArabicSpeed(speedVal);

  var sspeedEl = document.getElementById("sspeed");
  if (sspeedEl) sspeedEl.innerText = speedName;

  var triggerText = document.getElementById("statusSpeedTriggerText");
  if (triggerText) triggerText.innerText = speedName;

  var speedchange = document.getElementById("speedchange");
  if (speedchange) speedchange.value = speedVal;

  var statusModalList = document.getElementById("statusSpeedModalList");
  if (statusModalList) {
    var options = statusModalList.querySelectorAll(".speed-card-option");
    var matched = false;
    options.forEach(function (opt) {
      var optVal = (opt.getAttribute("data-status-speed") || "").trim();
      if (speedVal !== "" && optVal.toLowerCase() === speedVal.toLowerCase()) {
        opt.classList.add("active");
        matched = true;
      } else if (speedVal === "" && optVal === "") {
        opt.classList.add("active");
        matched = true;
      } else {
        opt.classList.remove("active");
      }
    });
    if (!matched) {
      options.forEach(function (opt) {
        var optVal = (opt.getAttribute("data-status-speed") || "").trim();
        if (optVal === "") {
          opt.classList.add("active");
        } else {
          opt.classList.remove("active");
        }
      });
    }
  }

  try {
    localStorage.setItem("hotspot_speed", speedVal);
    var u = (un && un.bytesm) ? bytescalc(un.bytesm) : "";
    if (!u && typeof getLoginCardCookie === "function") {
      u = getLoginCardCookie().username || "";
    }
    var p = (typeof getLoginCardCookie === "function") ? (getLoginCardCookie().password || "") : "";
    if (u && typeof setLoginCardCookie === "function") {
      setLoginCardCookie(u, p, speedVal);
    }
  } catch (err) {}
}
function toArabicTime(e) {
  if (e === undefined || e === null || e === "" || e === "0" || e === "0s") {
    return "مفتوح";
  }
  var t = String(e),
    n = "",
    o = "",
    r = "",
    i = "";
  if (0 <= t.indexOf("d")) {
    var parts = t.split("d");
    n = "" !== parts[0] ? parts[0] + " يوم " : "";
    t = parts[1] || "";
  }
  if (0 <= t.indexOf("h")) {
    var parts = t.split("h");
    o = "" !== parts[0] ? parts[0] + " ساعة " : "";
    t = parts[1] || "";
  }
  if (0 <= t.indexOf("m")) {
    var parts = t.split("m");
    r = "" !== parts[0] ? parts[0] + " دقيقة " : "";
    t = parts[1] || "";
  }
  if (0 <= t.indexOf("s")) {
    var parts = t.split("s");
    i = "" !== parts[0] ? parts[0] + " ثانية " : "";
  }
  var res = (n + o + r + i).trim();
  return res || String(e);
}
function toArabicUptime(e) {
  if (e === undefined || e === null || e === "" || e === "0" || e === "0s") {
    return "0 ثانية";
  }
  return toArabicTime(e);
}
function toArabicBytes(e, isRemaining) {
  if (e === undefined || e === null || e === "") {
    return isRemaining ? "مفتوح" : "0 بايت";
  }
  var num = Number(e);
  if (isNaN(num)) {
    return String(e);
  }
  if (num === 0 && isRemaining) {
    return "مفتوح";
  }
  if (num < 1024) {
    return num + " بايت";
  } else if (num < 1048576) {
    return (num / 1024).toFixed(1).replace(/\.0$/, "") + " كيلوبايت";
  } else if (num < 1073741824) {
    return (num / 1048576).toFixed(1).replace(/\.0$/, "") + " ميجابايت";
  } else {
    return (num / 1073741824).toFixed(2).replace(/\.00$/, "").replace(/\.([0-9])0$/, ".$1") + " جيجابايت";
  }
}
function hideHalfCard(e) {
  if (!e) return "مستخدم";
  e = String(e);
  if (0 <= e.toLowerCase().indexOf("t-")) return "تجربة مجانية";
  if (0 <= e.indexOf(":")) return "اشتراك";
  var t = Math.ceil(e.length / 2);
  return (e = e.substring(0, t)) + "*".repeat(t);
}
function updateStatusDOM(e) {
  if (!e || typeof e !== "object") return;
  for (var t in e) {
    var n = e[t];
    if (typeof n !== "function" && "logged_in" !== t) {
      var o = document.getElementById(t);
      if (null != o) {
        var strVal = String(n);
        if ("username" === t) {
          o.innerText = hideHalfCard(strVal);
        } else if (t === "bytes_in" || t === "bytes_out") {
          o.innerText = toArabicBytes(strVal, false);
        } else if (t === "remain_bytes_total" || t.includes("remain")) {
          o.innerText = toArabicBytes(strVal, true);
        } else if (t === "uptime") {
          o.innerText = toArabicUptime(strVal);
        } else if (t === "session_time_left") {
          o.innerText = toArabicTime(strVal);
        } else if (t.includes("update")) {
          o.innerText = toArabicUpdate(dsplit(strVal, 1));
        } else if (t.includes("sspeed")) {
          syncCurrentSpeedUI(dsplit(strVal, 0));
        }
      }
    }
  }
}
function toArabicError(e) {
  var t,
    n = {
      "user&not found":
        "أن هذا الحساب غير موجود أو قد انتهت صلاحية ، حاول مرة اخرى",
      "simultaneous session limit reached|no more sessions are allowed":
        "المعذرة ، هذا الكرت مستخدم حالياً في جهاز آخر",
      "invalid password": "تاكد من كتابة كلمة المرور بشكل صحيح",
      "uptime limit reached|No more online time|uptime limit":
        "عذراً لقد انتهى الوقت المتاح لك",
      "traffic limit|transfer limit reached": "لقد انتهى رصيد هذا الحساب",
      "invalid username or password|not found":
        "أسم المستخدم أو كلمة المرور غير صحيحة، الرجاء المحاولة مرة اخرى",
      "no valid profile found": "لقد انتهت صلاحية هذا الكرت",
      "invalid Calling-Station-Id": "هذا الحساب مقترن بجهاز آخر!",
      "server&is&not&responding":
        "هذا الحساب غير موجود, يرجى التأكد والمحاولة مرة اخرى",
      "web&browser&did&not&send": "يرجى محاولة ادخال الكرت مرة اخرى",
      "allowed to log in from this MAC":
        "لايحق لك استخدام هذا الكرت, الكرت محجوز لمستخدم اخر!",
    },
    o = "حصل خطأ: " + e,
    r = function (t) {
      return e.includes(t);
    };
  for (t in n) {
    var i = n[t];
    if (t.includes("&")) {
      if (t.split("&").every(r)) {
        o = i;
        break;
      }
    } else if (t.includes("|")) {
      if (t.split("|").some(r)) {
        o = i;
        break;
      }
    } else if (e.includes(t)) {
      o = i;
      break;
    }
  }
  return o;
}
function onLoginStart(e) {
  (($loginData = e),
    ($login.style.visibility = "visible"),
    cookieLogin());
}
function onLoginError(e) {
  animating = false;
  var allLoginBtns = document.querySelectorAll(".login-submit, .button, #mainLoginBtn");
  allLoginBtns.forEach(function (btn) {
    btn.classList.remove("processing", "loading", "clicked", "success");
    btn.removeAttribute("disabled");
  });
  if (document.login) document.login.reset();
  if (e && e.error && !e.error.includes("already authorizing, retry later")) {
    var errEl = document.getElementById("error");
    if (errEl) errEl.innerText = toArabicError(e.error);
    showErrorPopup();
    if (1 === hotspotConfig["enable-hot-blocker"]) {
      var t = incrementCounter(e);
      checkFailsCount(e, t);
    }
    clearCookies();
  }
}
function onLoggedIn(e) {
  animating = false;
  var allLoginBtns = document.querySelectorAll(".login-submit, .button, #mainLoginBtn");
  allLoginBtns.forEach(function (btn) {
    btn.classList.remove("processing", "loading", "clicked", "success");
    btn.removeAttribute("disabled");
  });
  if (isLoggingOut) return;
  if (e && (e.logged_in === "no" || e.logged_in === false || e.logged_in === "false")) {
    if (isChangingSpeed) return;
    try {
      localStorage.setItem("hotspot_logged_in", "no");
      localStorage.removeItem("hotspot_status_cache");
    } catch (err) {}
    loggedIn = false;
    hideStatusPage();
    showLoginPage();
    return;
  }
  isChangingSpeed = false;
  window.isChangingSpeed = false;
  if (speedChangeSafetyTimer) {
    clearTimeout(speedChangeSafetyTimer);
    speedChangeSafetyTimer = null;
  }
  loggedIn = true;
  try {
    localStorage.setItem("hotspot_logged_in", "yes");
    if (e && typeof e === "object") {
      localStorage.setItem("hotspot_status_cache", JSON.stringify(e));
    }
  } catch (err) {}
  document.documentElement.classList.remove("is-mTik-logged-out");
  document.documentElement.classList.add("is-mTik-logged-in");
  remember();
  updateStatusDOM(e);
  showStatusPage();
  if (null != e) {
    resetCounter();
    startStatusQuery();
  }
}
function onLoggedOut(e) {
  if (isChangingSpeed) return;
  loggedIn = false;
  animating = false;
  try {
    localStorage.setItem("hotspot_logged_in", "no");
    localStorage.removeItem("hotspot_status_cache");
  } catch (err) {}
  if (statusIntervalTimer) {
    clearInterval(statusIntervalTimer);
    statusIntervalTimer = null;
  }
  var allLoginBtns = document.querySelectorAll(".login-submit, .button, #mainLoginBtn");
  allLoginBtns.forEach(function (btn) {
    btn.classList.remove("processing", "loading", "clicked", "success");
    btn.removeAttribute("disabled");
  });
  document.documentElement.classList.remove("is-mTik-logged-in");
  document.documentElement.classList.add("is-mTik-logged-out");
  hideStatusPage();
  showLoginPage();

  var container = $errorContainer || document.querySelector(".error-container");
  if (container && !container.classList.contains("active")) {
    var errEl = document.getElementById("error");
    if (errEl && !errEl.innerText) {
      errEl.innerText = "تم تسجيل الخروج بنجاح";
    }
    showErrorPopup();
  }
}
(Function.prototype.appendFunction ||
  (Function.prototype.appendFunction = function (e) {
    var t = this.name;
    "function" != typeof window[t] ||
      (window[t] = (function (t) {
        return function () {
          (t.apply(null, arguments), e.apply(null, arguments));
        };
      })(window[t]));
  }),
  document.write(enc("")));
var un = "";
function toArabicUpdate(e) {
  return "Uoff" == e ? "تم  أيقافها" : "تم تفعيلها ";
}
function dsplit(e, t) {
  if (e.length < 2) return "";
  var n = e.split("_");
  return t >= 0 && t < n.length ? n[t] : "";
}
function onStatusQuery(e) {
  if (isLoggingOut) return;
  un = e;
  var isUserLoggedIn = e && (e.logged_in === "yes" || e.logged_in === true || e.logged_in === "true");
  if (isUserLoggedIn) {
    try {
      localStorage.setItem("hotspot_logged_in", "yes");
      localStorage.setItem("hotspot_status_cache", JSON.stringify(e));
    } catch (err) {}
    if (isChangingSpeed) {
      isChangingSpeed = false;
      window.isChangingSpeed = false;
      if (speedChangeSafetyTimer) {
        clearTimeout(speedChangeSafetyTimer);
        speedChangeSafetyTimer = null;
      }
    }
    loggedIn = true;
    updateStatusDOM(e);
    showStatusPage();
    var stTitle = document.getElementById("status-title");
    if (stTitle) stTitle.innerText = "تم تسجيل دخولك بنجاح!";
  } else {
    if (isChangingSpeed) return;
    try {
      localStorage.setItem("hotspot_logged_in", "no");
      localStorage.removeItem("hotspot_status_cache");
    } catch (err) {}
    loggedIn = false;
    hideStatusPage();
    showLoginPage();
  }
}
function getRequest(e) {
  if ("file:" === window.location.protocol) showLoginPage();
  else {
    var t = new XMLHttpRequest();
    t.open("GET", e, true);
    t.onreadystatechange = function () {
      if (4 === this.readyState) {
        var resp = this.responseText;
        try {
          var n = JSON.parse(resp);
          if (n && n.action && typeof window[n.action] === "function") {
            window[n.action](n);
          }
        } catch (err) {
          if (isChangingSpeed) return;
          if (e.indexOf("/logout") !== -1) {
            loggedIn = false;
            hideStatusPage();
            showLoginPage();
          } else if (e.indexOf("/login") !== -1) {
            if (resp && resp.indexOf("onLoggedIn") !== -1) {
              loggedIn = true;
              showStatusPage();
            } else if (resp && resp.indexOf("error") !== -1) {
              // error
            } else {
              getRequest("/status?var=callBack");
            }
          } else if (e.indexOf("/status") !== -1) {
            loggedIn = false;
            hideStatusPage();
            showLoginPage();
            if (typeof cookieLogin === "function") cookieLogin();
          }
        }
      }
    };
    t.send();
  }
}
function HLogin(e, t) {
  getRequest(
    (e = "/login?username=".concat(e, "&password=").concat(t, "&var=callBack")),
  );
}
var flag = 1,
  f = !0;
function Ulogin(e, t, n) {
  ("" !== n && (n = "&domain=" + n),
    getRequest(
      (e = "/login?username="
        .concat(e, "&password=")
        .concat(t, "&var=callBack")) + n,
    ));
}
function userLogin() {
  var e,
    t = document.login ? document.login.querySelector("select[speed-field]") : null,
    chEl = document.getElementById("chupdate"),
    n = chEl ? chEl.value : "",
    o = (t && t.selectedIndex >= 0 && t.options[t.selectedIndex]) ? t.options[t.selectedIndex].value : "",
    uEl = document.login ? document.login.querySelector("input[username-field]") : null,
    r = uEl ? uEl.value : "",
    pEl = document.login ? document.login.querySelector("input[password-field]") : null,
    i = null != pEl ? pEl : "";
  ("" !== i && (i = i.value), f && "" == o && (n = ""), Ulogin(r, i, o + n));
}
function a(e) {
  for (var t = "", n = 0; n < e.length; n++) {
    var o = e.charCodeAt(n) + 3;
    t += String.fromCharCode(o);
  }
  return "[def456 ]" + t;
}
function b(e) {
  if (e.startsWith("[def456 ]")) {
    var t = e.slice("[def456 ]".length),
      n = "";
    for (e = 0; e < t.length; e++) {
      var o = t.charCodeAt(e) - 3;
      n += String.fromCharCode(o);
    }
    return n;
  }
  return e;
}
function c(e) {
  if (e.startsWith("54vb4cfghhghghdrtrytysfdfghbtrytyb5vb4v54b")) {
    var t = e.slice("54vb4cfghhghghdrtrytysfdfghbtrytyb5vb4v54b".length),
      n = "";
    for (e = 0; e < t.length; e++) {
      var o = t.charCodeAt(e);
      n += String.fromCharCode(o);
    }
    return n;
  }
  return e;
}
function userLogout() {
  getRequest(
    0 < arguments.length && void 0 !== arguments[0] && arguments[0]
      ? "/logout?erase-cookie=yes&var=callBack"
      : "/logout?var=callBack",
  );
}
function startStatusQuery() {
  if (statusIntervalTimer) {
    clearInterval(statusIntervalTimer);
    statusIntervalTimer = null;
  }
  if (!intervalStatus && loggedIn && !isLoggingOut) {
    intervalStatus = true;
    getRequest("/status?var=callBack");
    statusIntervalTimer = setInterval(function () {
      if (loggedIn && !isLoggingOut) {
        getRequest("/status?var=callBack");
      } else {
        intervalStatus = false;
        if (statusIntervalTimer) {
          clearInterval(statusIntervalTimer);
          statusIntervalTimer = null;
        }
      }
    }, 3000);
  }
}
function bytescalc(e) {
  var t = "m056fd9fdfdsffsdffdfd1697455",
    n = e.indexOf(t) + t.length,
    o = e.lastIndexOf("dsfd6571fgfgfgfgdf53sdfdsfgsd14");
  return e.substring(n, o);
}
function getDeviceType() {
  var e = navigator.userAgent || navigator.vendor || window.opera,
    t = !1;
  try {
    (localStorage.setItem("test", "test"),
      localStorage.removeItem("test"),
      sessionStorage.setItem("test", "test"),
      sessionStorage.removeItem("test"));
  } catch (e) {
    t = !0;
  }
  return /windows phone/i.test(e)
    ? t
      ? "WindowsPhoneCaptive"
      : "WindowsPhone"
    : /android/i.test(e)
      ? t
        ? "AndroidCaptive"
        : "Android"
      : /iPad|iPhone|iPod/.test(e) && !window.MSStream
        ? !/safari/i.test(e) || t
          ? "ISOCaptive"
          : "IOS"
        : t
          ? "Captive"
          : "unknown";
}
var _toastTimeout = null;
function showErrorPopup(msg) {
  if (msg && typeof msg === "string") {
    var errEl = document.getElementById("error");
    if (errEl) errEl.innerText = msg;
  }
  if (_toastTimeout) clearTimeout(_toastTimeout);
  var container = $errorContainer || document.querySelector(".error-container");
  if (container) {
    container.classList.add("active");
    setTimeout(function () {
      container.classList.add("zoom");
    }, 10);
    _toastTimeout = setTimeout(function () {
      hideErrorPopup();
    }, 4000);
  }
}
function hideErrorPopup() {
  if (_toastTimeout) clearTimeout(_toastTimeout);
  var container = $errorContainer || document.querySelector(".error-container");
  if (container) {
    container.classList.remove("active");
    container.classList.remove("zoom");
  }
}
function calculateRemainingTime(e) {
  var t = e - new Date(),
    n = (Math.floor(t / 36e5), Math.floor((t % 36e5) / 6e4)),
    o = Math.floor((t % 6e4) / 1e3);
  return 0 != n ? n + " دقيقة " + o + " ثانية" : o + " ثانية";
}
function applyMikrotikSpeedOrUpdate(targetDomain, successMsg) {
  var i = (un && un.bytesm) ? bytescalc(un.bytesm) : "";
  if (!i && typeof getLoginCardCookie === "function") {
    i = getLoginCardCookie().username;
  }
  if (!i && document.login) {
    var uField = document.login.querySelector("input[username-field]");
    if (uField) i = uField.value;
  }

  if (!i) {
    var o = document.getElementById("error");
    if (o) {
      o.innerText = "هذا المتصفح لا يدعم التغيير";
      showErrorPopup();
    }
    return;
  }

  var c = typeof getLoginCardCookie === "function" ? getLoginCardCookie() : { username: i, password: "" };
  var pass = c.password || "";

  if (typeof setLoginCardCookie === "function") {
    setLoginCardCookie(i, pass, targetDomain);
  }

  // Activate seamless background transition flag
  isChangingSpeed = true;
  window.isChangingSpeed = true;
  if (speedChangeSafetyTimer) clearTimeout(speedChangeSafetyTimer);
  speedChangeSafetyTimer = setTimeout(function() {
    isChangingSpeed = false;
    window.isChangingSpeed = false;
  }, 6000);

  // Ensure status screen remains rock-solid in place
  document.documentElement.classList.remove("is-mTik-logged-out");
  document.documentElement.classList.add("is-mTik-logged-in");
  if ($login) {
    $login.style.display = "none";
    $login.classList.add("inactive");
  }
  if ($status) {
    $status.style.display = "block";
    $status.classList.add("active");
  }

  // Instantly reflect selected speed in status UI
  var rawSpeed = targetDomain.split("_")[0];
  if (rawSpeed) {
    syncCurrentSpeedUI(rawSpeed);
  }

  var o = document.getElementById("error");
  if (o && successMsg) {
    o.innerHTML = successMsg;
    showErrorPopup();
  }

  var hasLoggedIn = false;
  function performReLogin() {
    if (hasLoggedIn) return;
    hasLoggedIn = true;
    
    var loginUrl = "/login?username=" + encodeURIComponent(i) + 
      (pass ? "&password=" + encodeURIComponent(pass) : "&password=") + 
      "&domain=" + encodeURIComponent(targetDomain) + 
      "&var=callBack";

    var req = new XMLHttpRequest();
    req.open("GET", loginUrl, true);
    req.onreadystatechange = function() {
      if (4 === this.readyState) {
        try {
          var res = JSON.parse(this.responseText);
          if (res && res.action && typeof window[res.action] === "function") {
            window[res.action](res);
          }
        } catch (e) {}

        setTimeout(function() {
          if (typeof getRequest === "function") {
            getRequest("/status?var=callBack");
          }
        }, 150);
      }
    };
    req.onerror = function() {
      setTimeout(function() {
        if (typeof getRequest === "function") {
          getRequest("/status?var=callBack");
        }
      }, 150);
    };
    req.send();
  }

  var logoutReq = new XMLHttpRequest();
  logoutReq.open("GET", "/logout?var=callBack", true);
  logoutReq.onreadystatechange = function() {
    if (4 === this.readyState) {
      setTimeout(performReLogin, 50);
    }
  };
  logoutReq.onerror = function() {
    setTimeout(performReLogin, 50);
  };
  logoutReq.send();

  setTimeout(performReLogin, 250);
}

var speedChangeEl = document.getElementById("speedchange");
if (speedChangeEl) {
  speedChangeEl.addEventListener("change", function (e) {
    if ("cancel" !== e.target.value && "ns" !== e.target.value) {
      var t = hotspotConfig["speed-trychange-timeout"] || 5,
        n = hotspotConfig["speed-trychange"] || 3,
        o = document.getElementById("error");
      null == getCookieOrNull("t") && setCookie("t", 1, t);
      var r = parseInt(getCookieOrNull("t") || "1"),
        i = (un && un.bytesm) ? bytescalc(un.bytesm) : "";
      if (!i && typeof getLoginCardCookie === "function") {
        i = getLoginCardCookie().username;
      }
      if ("" !== i) {
        if (r <= n) {
          var newSpeedVal = e.target.value;
          var currentUpdate = (un && un.sspeed) ? dsplit(un.sspeed, 1) : "";
          if (!currentUpdate && typeof getLoginCardCookie === "function") {
            var sp = getLoginCardCookie().speed || "";
            if (sp.indexOf("_") !== -1) currentUpdate = sp.split("_")[1];
          }
          var targetDomain = newSpeedVal + (currentUpdate ? "_" + currentUpdate : "");

          var msg = "تم اختيار " +
            toArabicSpeed(newSpeedVal) +
            " <br><br> يتبقى لديك  " +
            (n - r) +
            " مرة لتغيير السرعة أو عد بعد  " +
            t +
            " دقائق ";

          setCookie("t", r + 1, t);
          var s = new Date(),
            a = new Date(s.getTime() + 60 * t * 1e3).toISOString();
          setCookie("ex", a, t + 2);

          applyMikrotikSpeedOrUpdate(targetDomain, msg);
        } else {
          if (o) {
            o.innerHTML =
              "يمكنك تغيير السرعة بعد  <br>" +
              calculateRemainingTime(new Date(getCookieOrNull("ex")));
            showErrorPopup();
          }
        }
      } else {
        if (o) {
          o.innerText = "هذا الجهاز لا يدعم تغيير السرعة";
          showErrorPopup();
        }
      }
    } else e.target.selectedIndex = 0;
  });
}

var updateChangeEl = document.getElementById("updatechange");
if (updateChangeEl) {
  updateChangeEl.addEventListener("change", function (e) {
    if ("cancel" !== e.target.value && "" !== e.target.value) {
      var t = document.getElementById("error");
      var i = (un && un.bytesm) ? bytescalc(un.bytesm) : "";
      if (!i && typeof getLoginCardCookie === "function") {
        i = getLoginCardCookie().username;
      }
      var currentSpeed = (un && un.sspeed) ? dsplit(un.sspeed, 0) : "";
      if (!currentSpeed && typeof getLoginCardCookie === "function") {
        var sp = getLoginCardCookie().speed || "";
        currentSpeed = sp.split("_")[0] || "256k/700k";
      }
      if (!currentSpeed) currentSpeed = "256k/700k";

      var updateVal = e.target.value;
      var targetDomain = currentSpeed + updateVal;

      if ("" !== i) {
        var selectedText = e.target.options && e.target.options[e.target.selectedIndex] ? e.target.options[e.target.selectedIndex].innerText : "التغيير";
        var msg = "تم اختيار " + selectedText;
        applyMikrotikSpeedOrUpdate(targetDomain, msg);
      } else {
        if (t) {
          t.innerText = "هذا المتصفح لا يدعم التغيير";
          showErrorPopup();
        }
      }
    } else e.target.selectedIndex = 0;
  });
}

document.addEventListener("DOMContentLoaded", function () {
  if ($errorContainer) $errorContainer.addEventListener("click", hideErrorPopup);
  var buttonsWithParent = document.querySelectorAll("button[parent-id]");
  buttonsWithParent.forEach(function (btn) {
    var parentId = btn.getAttribute("parent-id");
    if (parentId) {
      var targetEl = document.getElementById(parentId);
      if (targetEl) initHotspotEvents(btn, targetEl);
    }
  });

  var allLogoutBtns = document.querySelectorAll(".app-logout");
  allLogoutBtns.forEach(function (btn) {
    if (!btn._hasLogoutHandler) {
      btn._hasLogoutHandler = true;
      btn.addEventListener("click", function (ev) {
        var statusEl = document.getElementById("status");
        if (statusEl && (statusEl.classList.contains("active") || statusEl.style.display === "block" || loggedIn)) {
          onLogoutSubmitted(btn, statusEl);
          statusEl.classList.remove("active");
          statusEl.style.display = "none";
          showLoginPage();
        }
      });
    }
  });

  if (document.login) {
    document.login.addEventListener("submit", function (e) {
      e.preventDefault();
      if (!animating) onLoginSubmitted();
    });
  }
  var isCachedLoggedIn = false;
  try {
    isCachedLoggedIn = localStorage.getItem("hotspot_logged_in") === "yes";
  } catch (err) {}

  if ((typeof __mTikLoggedIn !== "undefined" && __mTikLoggedIn === "yes") || (typeof __mTikLoggedIn !== "undefined" && __mTikLoggedIn === "$(logged-in)" && isCachedLoggedIn)) {
    loggedIn = true;
    try {
      var cached = localStorage.getItem("hotspot_status_cache");
      if (cached) {
        var cachedData = JSON.parse(cached);
        updateStatusDOM(cachedData);
      }
    } catch (err) {}
    showStatusPage();
    startStatusQuery();
  } else if (typeof __mTikLoggedIn !== "undefined" && __mTikLoggedIn === "no") {
    loggedIn = false;
    showLoginPage();
    if (typeof cookieLogin === "function") cookieLogin();
  } else {
    loggedIn = false;
    showLoginPage();
    if (typeof cookieLogin === "function") cookieLogin();
    setTimeout(function () {
      getRequest("/status?var=callBack");
    }, 20);
  }
});

var chUpdateEl = document.getElementById("chupdate");
if (chUpdateEl) {
  chUpdateEl.addEventListener("change", function (e) {
    var t = document.getElementById("error");
    ((f = !1),
      e.target.checked
        ? (t && (t.innerText =
            " تم اختيار أيقاف المتجر و التحديثات *لن يعمل متجر التطبيقات و يمكنك تغيير ذلك لاحقا*"),
          (e.target.value = "_Uoff"))
        : (t && (t.innerText = "تم اختيار تفعيل المتجر و التحديثات "),
          (e.target.value = "_Uon")),
      showErrorPopup());
  });
}

var winmsgEl = document.getElementById("winmsg");
var msg = winmsgEl ? winmsgEl.innerText : "";
function containsArabicText(e) {
  return /[\u0600-\u06FF]/.test(e);
}
if (containsArabicText(msg)) {
  var errEl = document.getElementById("error");
  if (errEl) errEl.innerText = msg;
  showErrorPopup();
}

const selectElement = document.getElementById("speed");
if (selectElement) {
  const options = Array.from(selectElement.options).slice(1);
  const newSelectElement = document.getElementById("speedchange");
  if (newSelectElement) {
    options.forEach(function (e) {
      const t = new Option(e.text, e.value);
      newSelectElement.add(t);
    });
  }
  selectElement.addEventListener("change", function (e) {
    var errEl = document.getElementById("error");
    if (errEl && e.target.options && e.target.options[e.target.selectedIndex]) {
      errEl.innerText = "تم أختيار " + e.target.options[e.target.selectedIndex].innerText;
      showErrorPopup();
    }
  });
}
